---
title: CSV export crashes on large datasets
status: backlog
priority: low
---

# CSV export crashes on large datasets

Exporting more than 10,000 rows causes the browser to freeze.

## Solution ideas

- Stream the export
- Use Web Workers
- Server-side generation with download link
